문제 : 숫자 뒤집기 프로그램 만들기
- 사용자로부터 양의 정수를 하나 입력 받아 해당 숫자를 뒤집어서 출력하는 프로그램을 작성하시오
- 단, 문자열 변환을 사용하지 말고 반복문과 나눗셈/나머지 연산을 이용해 구현하시오

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.print("양의 정수를 입력하세요: ");
        int n = sc.nextInt();

        int reversed = 0;

        while (n > 0) {
            int digit = n % 10;
            reversed = reversed * 10 + digit;
            n = n / 10;
        }

        System.out.println("뒤집은 숫자: " + reversed);

        sc.close();
    }
}
